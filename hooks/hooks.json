{
  "description": "outlook: ask before any Bash command that sends mail, meeting invitations, responses, cancellations or updates",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/send-gate.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
